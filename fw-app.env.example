# Copy to /etc/fw-os/fw-app.env and set deployment-specific values.
# This file contains non-secret deployment configuration. Store credentials as
# podman secrets named after their environment variables (FW_AUTH_*).
FW_AUTH_GITHUB_ORG=your-github-org
FW_SERVER_FQDN=your-device.your-tailnet.ts.net
